import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.alexishayat.angulai',
  appName: 'angul-ai-talks',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
